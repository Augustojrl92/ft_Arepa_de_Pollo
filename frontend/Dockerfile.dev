FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./

EXPOSE 3000

CMD ["sh", "-lc", "if [ ! -d node_modules ] || [ -z \"$(ls -A node_modules 2>/dev/null)\" ]; then npm ci --cache /tmp/.npm --no-audit --no-fund && rm -rf /tmp/.npm /root/.npm; fi && npm run dev"]
